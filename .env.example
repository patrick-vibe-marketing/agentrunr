# =============================================================================
# JobRunr Agent — Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values.
# Never commit .env to version control!
#
# Usage with Maven:
#   source .env && mvn spring-boot:run
# Or use a tool like dotenv-java, direnv, or IntelliJ's EnvFile plugin.
# =============================================================================

# --- LLM Providers (at least one required) ---

# OpenAI
OPENAI_API_KEY=sk-your-openai-key

# Anthropic (optional)
ANTHROPIC_API_KEY=
ANTHROPIC_ENABLED=false

# Ollama (optional, local)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2
OLLAMA_ENABLED=false

# To disable auto-configuration for unused providers, leave these as-is:
# EXCLUDE_OLLAMA=org.springframework.ai.autoconfigure.ollama.OllamaAutoConfiguration
# EXCLUDE_ANTHROPIC=org.springframework.ai.autoconfigure.anthropic.AnthropicAutoConfiguration

# --- Channels ---

# Telegram Bot
TELEGRAM_ENABLED=false
TELEGRAM_BOT_TOKEN=
TELEGRAM_ALLOWED_USERS=

# --- Security ---

# API key for REST endpoint authentication (optional, disables auth if empty)
AGENT_API_KEY=

# --- MCP (Model Context Protocol) ---

# Personal Calendar (n8n MCP gateway)
PERSONAL_CALENDAR_MCP_ENABLED=false
PERSONAL_CALENDAR_MCP_URL=
PERSONAL_CALENDAR_MCP_PASSWORD=

# --- Built-in Tools ---

# Brave Search API key (for web_search tool)
BRAVE_API_KEY=

# Workspace restrictions
TOOLS_RESTRICT_WORKSPACE=true
TOOLS_WORKSPACE=./workspace
TOOLS_SHELL_TIMEOUT=30
TOOLS_MAX_OUTPUT=65536

# --- Agent ---

# Heartbeat (periodic task checking)
HEARTBEAT_ENABLED=true
HEARTBEAT_INTERVAL=30
HEARTBEAT_FILE=./HEARTBEAT.md

# Memory storage path
AGENT_MEMORY_PATH=./data/memory

# --- Server ---

SERVER_PORT=8090
